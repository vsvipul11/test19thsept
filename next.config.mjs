// next.config.mjs
export const nextConfig = {
  // Remove the custom distDir if you want to use the default '.next' directory
  // distDir: 'build',
  
  typescript: {
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
